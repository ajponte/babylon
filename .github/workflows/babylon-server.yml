name: babylon-server

on:
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.13"]

    steps:
    - uses: actions/checkout@v4
    - name: Download API spec artifact
      uses: actions/download-artifact@v4
      with:
        name: api-spec-17652643510
        path: ./api-specs
        # The repository where the artifact was uploaded
        repository: ajponte/babylon_api_spec
        # The run ID of the workflow that uploaded the artifact
        run-id: 17652643510
        # A Personal Access Token (PAT) with 'repo' scope, stored as a secret in this repository
        github-token: ${{ secrets.PAT_TOKEN }}
    - name: Set up Python 3.13
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install tox
        pip install poetry 
        # Install poetry dependencies only.
        poetry install
    - name: Pylint checks
      run: |
        tox -e lint
    - name: Formatting checks
      run: |
        tox -e format
    - name: Pytest
      run: |
        tox -e pytest
